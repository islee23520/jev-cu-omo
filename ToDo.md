# 当前模块：OmO 原生 macOS CUA 集成

- [x] 修复目标替换、重复无效动作及 P0 假成功，加入确定性回归测试。
- [x] 使用 cua-driver 当前结构化快照和 element_token，动作前后重新观测。
- [x] 注册 OmO jev_cu 工具；默认 dry-run，明确结果核验，错误不得报告为成功。
- [x] 独立 OmO 实例加载并实际调用扩展。
- [ ] 使用真实 TypeSafe Jev API 完成 macOS GUI 质量门槛。
- [x] 集成本地 Laya 0.3.4 后端、Python 3.12 安装脚本、候选上限和安全门；记录真实 P0 结果。
- [x] 发布 Zcode 审核的 Blender 女角色脸/颈连续拓扑修复命令与 RED→GREEN 证据。
- [ ] 全部通过后移除 Jev-like 运行路径，安装扩展和技能，验证、提交并推送。

## 发布门槛

1. 上游测试及所有新增回归测试零失败，不跳过测试。
2. 正式 OmO 进程实际发现并调用 jev_cu，而不是仅模拟 registerTool。
3. 真正 Jev API：全部 P0 标注用例通过；记录准确率、耗时、token 和错误。
4. macOS 实际 GUI：至少 5 个短任务全部以新快照的精确值验证成功；
   至少包含多步计算和文本输入；未经核验的 done、接管、超时均不计成功。
5. dry-run 零动作；错误/低置信度/不支持动作不得执行；无效重复动作最多 2 次。
6. 不改变用户前台应用，不写用户文档；仅使用测试用应用状态和临时文件。
7. 检查独立配置、密钥不入 Git、生产配置未被测试污染。

API 密钥或真实 GUI 验证缺失时，状态是未通过，不安装生产版本。

当前证据见 [QA.md](QA.md)。TypeSafe 登录被 invite-only 拒绝，未生成 API key。
用户提出 Windows 自托管公开权重模型作为替代；已确认可行，等待选择后才修改
决策后端及相关验收标准。替代模型不称为真实 Jev，也不复用已要求废弃的 Jev-like。
